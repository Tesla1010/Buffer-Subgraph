type PlayerResult @entity {
  id: String!
  playerAddress: Bytes!
  wager: BigInt!
  payout: BigInt!
  tokenAddress: Bytes!
  slotIDs: [Int!]!
  multipliers: [BigInt!]!
  payouts: [BigInt!]!
  numGames: BigInt!
}

type Counter @entity {
  id: String!
  counter: Int!
}